# Backend REST API base URL used by the browser application.
# Production builds require this value so localhost is never baked in accidentally.
# Use an absolute URL for separate frontend/backend hosts, or /api for same-origin hosting.
VITE_API_URL=http://localhost:5000/api
